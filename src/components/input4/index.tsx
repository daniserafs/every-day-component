import * as S from './style.css'

export type Input4Props = {
    children: React.ReactNode
}

const Input4 = ({children}: Input4Props) => (
    <div>
        <h1>{children}</h1>
    </div>
)

export default Input4