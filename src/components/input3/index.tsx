import * as S from './style.css'

export type Input3Props = {
    children: React.ReactNode
}

const Input3 = ({children}: Input3Props) => (
    <div>
        <h1>{children}</h1>
    </div>
)

export default Input3